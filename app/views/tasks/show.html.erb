<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<%= @task.completed ? 'Task is completed' : 'Task is not completed' %></p>
<%= link_to 'Edit', edit_task_path(@task) %>
<%= link_to "Delete", task_path(@task), method: :delete%>


<%= link_to 'Back to the tasks', tasks_path%>

